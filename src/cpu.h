#include <stdio.h>

int start();
int initialize(unsigned short int* pcPtr, unsigned char* delayPtr, unsigned char* soundPtr, unsigned short int* memPtr);
void font(unsigned short int* memPtr);
int game();
